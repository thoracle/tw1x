# TW1X has no runtime dependencies - pure Python!

# Development dependencies (optional)
# Install with: pip install -r requirements.txt
pytest>=7.0.0
pytest-cov>=3.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950
